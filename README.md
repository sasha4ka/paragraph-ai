# Paragraph AI 📚🤖

Интеллектуальный бот для платформы MAX, разработанный для помощи школьникам в работе с учебниками. Сервис позволяет загружать книги, формировать краткие конспекты по конкретным параграфам, проверять усвоение материала через интерактивные вопросы от нейросети через OpenRouter и готовиться к контрольным работам.

---

## 🌟 Ключевые возможности

* **📚 Управление книгами:** загрузка учебных материалов, структурированное хранение в профиле, выбор активного учебника и удаление неактуальных книг.


* **📝 Разбор параграфов:** автоматическое извлечение ключевых тезисов, дат, определений и понятий без лишней «воды».
* **🧠 Интерактивный опрос (Quiz):** генерация контрольных вопросов нейросетью по выбранному параграфу и диалоговая проверка ответов ученика.
* **🎯 Подготовка к контрольным:** выбор пула параграфов по предмету для генерации комплексной сводки и экспресс-тестирования.



---

## 📐 Архитектура меню и FSM

Навигация пользователя выстроена на базе конечного автомата (FSM):

```text
Главное меню
 ├── 📚 Мои книги (manage_books)[cite: 1]
 │    ├── 📖 Список книг -> выбор книги -> выбор параграфа -> Конспект и Вопросы[cite: 1]
 │    ├── ➕ Добавить книгу -> загрузка файла -> ввод названия[cite: 1]
 │    └── ❌ Удалить книгу -> выбор из списка -> подтверждение[cite: 1]
 ├── 📝 Подготовка (prep_test — выбор тем для сводного повторения)[cite: 1]
 └── 🎯 Контрольная (комплексный проверочный тест)[cite: 1]

```

---

## 🛠 Стек технологий

* **Язык разработки:** Python 3.12+
* **Пакетный менеджер:** [uv](https://github.com/astral-sh/uv?utm_source=gemini)
* **Фреймворк бота:** `maxapi`
* **База данных:** PostgreSQL
* **ORM и драйвер:** `SQLAlchemy` (асинхронный движок) + `asyncpg`
* **LLM-интеграция:** OpenRouter API через официальный клиент `openai`
* **Конфигурация:** `pydantic-settings`

---

## 📁 Структура проекта

```text
.
├── app/
│   ├── db/
│   │   ├── db.py               # Инициализация подключения к БД и сессий SQLAlchemy
│   │   └── models.py           # Декларативные модели таблиц (пользователи, книги, прогресс)
│   ├── handlers/               # Обработчики событий и команд бота
│   │   ├── manage_books/       # Логика раздела управления книгами (роутер, клавиатуры, утилиты)[cite: 1]
│   │   │   ├── keyboards.py
│   │   │   ├── router.py
│   │   │   └── utils.py
│   │   ├── start/              # Приветствие и стартовые сценарии
│   │   │   ├── keyboards.py
│   │   │   └── router.py
│   │   ├── learn_paragraph.py  # Конспектирование и опрос по параграфу
│   │   ├── main_menu.py        # Навигация по основному меню
│   │   ├── manage_books.py     # Обработка действий с учебниками
│   │   └── prep_test.py        # Сценарии подготовки к контрольным срезам
│   ├── logics/
│   │   └── parser.py           # Логика извлечения текста и структуры из книг
│   ├── app.py                  # Сборка экземпляра бота и регистрация роутеров
│   ├── keyboards.py            # Общие клавиатуры и элементы управления
│   ├── settings.py             # Настройки приложения и переменных окружения (Pydantic)
│   └── states.py               # Описание FSM-состояний пользователя[cite: 1]
├── cache/                      # Локальное хранилище и кэш обработанных файлов
├── main.py                     # Точка входа для запуска бота
├── pyproject.toml              # Конфигурация проекта и зависимости uv
└── uv.lock                     # Lock-файл зависимостей

```

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/sasha4ka/paragraph-ai.git
cd paragraph-ai

```

### 2. Синхронизация окружения через uv

Убедитесь, что у вас установлен `uv`. Для создания виртуального окружения и установки всех зависимостей выполните:

```bash
uv sync

```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими параметрами:

```env
# Бот MAX
BOT_TOKEN=your_maxapi_token_here

# База данных PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/paragraph_ai

# OpenRouter / OpenAI API
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct

```

### 4. Запуск бота

Запустите проект с помощью команды `uv`:

```bash
uv run python main.py

```
